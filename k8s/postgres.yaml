apiVersion: v1
kind: Pod
metadata:
  name: postgres-pod
  labels:
    name: postgres
    type: db
spec:
  containers:
    - name: postgres-container
      image: postgres:15
      env:
        - name: POSTGRES_USER
          value: postgres
        - name: POSTGRES_PASSWORD
          value: postgres

